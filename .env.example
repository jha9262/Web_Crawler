# ============================================================
# Frontend Environment Variables — copy to .env.local for dev
# NEVER commit .env.local or .env to version control
# ============================================================

# Backend API base URL (include /api/crawl path)
VITE_API_URL=http://localhost:8080/api/crawl
